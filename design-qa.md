# 0.2.6 / T46 侧栏开关与概览归属验收

日期：2026-09-15（本地）。版本：0.2.6；PRD 2.11 / T46。

在隔离 Desktop 2.0.3 Host + 离线适配器 + 当前源码候选的 Edge 实测中，1920×1000 会话容器 x=280…1920、width=1640：

- `conversation.session.header.utilities` 列表内先渲染 `Session log`（x=1741…1852），紧接着插件侧栏开关（x=1860…1892），确认开关不再位于标题旁的 actions 组。
- 概览卡由 `SessionOverviewCard` 在主会话 owner 正常文档流中右对齐，dock x=1574…1904、y=120…707；聊天滚动区 x=280…1920、width=1640，滚动条位于 x≈1912…1920，即在概览卡右侧。旧 0.2.5 的 `padding-right:354px` + absolute 定位不再存在。
- 窄屏继续按原有断点转为概览顶部回退入口；打开自有工作区或资源预览时概览隐藏、关闭恢复。

证据：`.verification/overview-2026-09-16T03-03-28-659Z/layout-t46-installed-verification.json`、`host.log`（0.2.6 mounted）。0.2.6 候选包已按收据 `.verification/desktop-install-2026-09-15-t46/installation-t46.json` 安装到本机 `desktop` Profile；实际窗口仍须完整退出 Desktop（含托盘）并重开后确认。

---

# 0.2.5 / T45 Codex 类宽工作区验收

日期：2026-09-15（本地）。版本：0.2.5；PRD 2.10 / T45。

## 宽工作区结果

插件自有 workspace surface 从公共 conversation header slot 锚定，排除左侧导航后按会话容器可用宽度默认约 70% 工作区 / 30% 聊天。1920×1000 实测会话区 x=280…1920、width=1640，工作区 x=772…1920、width=1148；1280×900 实测会话区 width=1000，工作区 width=680，聊天保留 320px。由于最小值约束，较窄宽度不承诺严格 70/30。宽屏分隔条可拖动，左右方向键每次调整，Home 恢复 70%，用户调整后的比例在关闭、资源切换和会话切换后保留。

可用宽度小于 740 CSS px 时，同一 surface 移到 `body` 作为全宽 drawer；390×844 实测 x=0、y=36、width=390、height=808，避开 Desktop 36px 顶栏且 `documentElement.scrollWidth` 不超过视口。聊天正文、标题栏和输入框仍可见并可操作。

打开自有工作区、输出集合、来源集合、文件/网页/附件预览或子智能体目录时，概览卡隐藏并释放其占位；关闭自有侧栏后概览恢复。选择“工具详情”交还宿主原生 details，`observeNativePane` 仅在 Desktop 2.0.3 已验证的三轨 DOM 结构下只读发布真实右列可见性；原生详情实际打开时概览隐藏，关闭后恢复。原生 details 宽度仍由 Host 管理，不能用插件 70/30 比例推断或设置。

证据：`.verification/overview-2026-09-16T02-03-01-333Z/`；截图：[宽工作区](docs/assets/wide-workspace.png)。本轮隔离 UI 没有模型调用、父会话轮次、用户会话写入或 ASAR 修改。安装收据和用户 Desktop 完整退出/重开后的加载状态见 `docs/DESKTOP-TRYOUT.md`。

---

# Desktop 小窗口回归验收

日期：2026-09-15（本地）。版本：0.2.2；PRD 2.7 / T42。

## 问题与根因

参考用户原图 `C:/Users/20825/AppData/Local/Temp/codex-clipboard-421c6a78-091c-44f0-9712-e618af0663a0.png`，尺寸1199×798。实际 Desktop 2.0.3 ASAR的兼容模式将 `#root` 放在36px顶栏下，并使用 `transform:translateZ(0)` 建立固定定位参照。旧概览把视口坐标当作该容器内部的fixed坐标，重复计算顶栏偏移。

隔离夹具通过正式客户端插件复用这组布局条件。旧封存0.2.1在相同尺寸下：卡片bottom=375.67、聊天滚动区top=347.67，重叠28px，复现用户问题。原0.2.1普通浏览器验证没有包含Desktop容器，因此此前“无重叠”结论不覆盖本缺陷。

## 修复与可见结果

- 窄屏概览作为标题之后的正常流区块，实际卡片高度直接占位；新版相同尺寸bottom=339.67、聊天top=351.67，间距12px。
- 以实际会话容器1080px为宽窄断点；监听标题及容器尺寸。宽屏卡片在右侧，聊天与卡片水平分离。
- 114条合成来源展开后在卡片内部滚动，不增加遮挡；长标题和父会话返回入口保持可用。
- 极窄窗口预览根据实际应用内容边界定位。修复前标题与关闭按钮被顶栏挡住；修复后390×844的预览top=36、bottom=844，关闭按钮可见并点击通过。
- 宽屏1920×1080原生预览位于x=1560.67…1920；概览x=1214…1544，互不覆盖。切回父会话后预览释放，概览仅一个。
- 保留默认常驻、无关闭按钮、整行悬停/聚焦和只读预览语义。本次没有新增外部模型调用。

## 验证范围

真实Desktop Host + 独立Profile + 离线适配器 + 最终生产编译文件，在Codex内置浏览器运行；Desktop标题栏与transform由独立夹具模拟，不等同于操作系统原生窗口实测。矩阵包括1199×798、1280×720、1920×1080、390×844以及1359/1360/1361px断点。跨窄屏的Host侧栏存在正常过渡动画，验收使用动画结束后的几何数据。初始版本仿真和修复版使用相同夹具与真实公开会话测试数据。

类型检查、构建、91文件 / 1,339项测试、lint、Host/client/companion smoke通过；新增冷会话与异步鉴权竞态回归及预览坐标/清理测试。

证据：`.verification/overview-022-desktop-wrapper-evidence.json`（实际ASAR样式依据）；旧版`.verification/overview-2026-09-16T00-57-22-966Z/`；新版`.verification/overview-2026-09-16T00-59-16-618Z/`。可携带截图：[小窗口修复](docs/assets/overview-responsive.png)。

用户截图的403具体请求没有捕获，不能断言只有一个原因。已复现并修复“冷持久会话没有live Agent却被拒绝只读bootstrap”的路径，保留同源/loopback边界，不恢复Agent或放开协调写。新错误文案区分来源拒绝与会话身份失效。

## 交付边界

不修改Desktop ASAR或正式会话；不提交、推送或公开发布。安装收据、回滚和现有用户窗口是否重开分别记录在[Desktop试用记录](docs/DESKTOP-TRYOUT.md)。Linux、其他Host版本、完整浏览器/终端/审查工具不在本次实测范围。

**布局回归结果：通过。**

真实Host冷会话补充验证：`.verification/overview-2026-09-16T01-04-02-389Z/cold-session-verification.json`。由正式AgentHandle自行创建并dispose隔离会话；bootstrap/overview/preview均200，协调写409 CONTROLLER_INACTIVE，Agent始终未恢复，离线providerCalls保持2、父turn保持1。测试Host已停止且端口释放。

资源终态：本次隔离Host的cleanup记录确认端口释放，浏览器测试标签已关闭，viewport override已恢复。0.2.2已安装到本机desktop Profile，实际用户窗口重开确认仍待完成。


## 0.2.3 / T43：独立侧栏入口与首页

用户参考图：`C:/Users/20825/AppData/Local/Temp/codex-clipboard-3451858f-d355-4444-b978-2ae5aec58efb.png`（侧栏首页）与`codex-clipboard-f8b571ca-286a-4cdc-9901-05383490c266.png`（右上角开关）。本次对应的是入口、开合关系及中央整行菜单，沿用DSH主题与图标，没有复制Codex未接入的工具或伪造可用状态。

真实Host和Desktop容器仿真证据：`.verification/overview-2026-09-16T01-13-20-243Z/`。1600×950实测：开关位于标题栏右侧，首页四行整条按钮居中；输出文件→文件Markdown预览→首页、114条来源集合、URL非法scheme校验、轮询后输入保留、工具详情恢复、顶部开关关闭/重开及切会话释放均通过。预览右栏x=1240.67…1600，顶边36px。关闭侧栏后概览仍只有一份并保持可见。

390×844首次检查发现长父会话来源将标题栏开关挤出视口；已补充根据标题栏按钮是否超出实际会话可视边界，将被挤出的入口移到概览卡顶部并隐藏原位置，实际点击可打开抽屉。抽屉top=36/bottom=844，无横向溢出，关闭按钮可操作。

来源截图与最终界面均已直接检查。可携带结果：[侧栏首页](docs/assets/sidebar-home.png)。布局继承DSH的原生右栏宽度，不冒充截图中完整侧边聊天、浏览器或PTY终端。首页资源使用当前会话真实投影，远程文件不交给本机预览。用户尚未完整重开Desktop的状态与安装分开记录。

源码/测试类型、构建、93文件 / 1,352项测试、lint与三类smoke通过。最终结果：通过。`final result: passed`。

最终补充：入口改为依据实际按钮边界判定是否移到概览顶部，避免单一宽度断点遗漏长标题情况。900、1199、390px实测入口均可点击；有效网址example.com成功显示Example Domain，非法scheme拒绝，浏览器error日志为空。

0.2.3资源终态：隔离Host已停止，cleanup确认50213端口释放，测试标签关闭及viewport恢复。本机安装成功，旧包与受保护回滚保留；未停止用户Desktop，未提交或推送。
# 0.2.4 原生子智能体增量验收

2026-09-15（本地日期）。来源是用户提供的 Codex 概览与子智能体列表截图，落实为 DSH 原生子智能体入口和分组列表，并保留独立委派任务。

数据使用实际 Desktop 2.0.3 Host 的正式子智能体 API，由隔离离线适配器生成，不修改用户历史或安装的 ASAR。最终运行目录 `.verification/overview-2026-09-16T01-47-44-377Z/`；首次功能跳转验证 `.verification/overview-2026-09-16T01-45-06-446Z/`。

- 概览显示 2 运行中、1 已结束 / 空闲、1 记录不可用，与真实 Host 目录一致；普通委派任务仍单独显示 1。
- 右侧列表按运行中、非运行、诊断记录分组，整行按钮包含图标、名称、状态与累计时间；诊断项禁用。标签优先于工作区回退标题。
- 真实完成历史及 continuable 会话均可用正式原生地址打开；父级面包屑可返回，切会话释放原侧栏，返回首页恢复四个现有入口。
- 390×844 下抽屉稳定边界 x=0、y=36、width=390、height=808，关闭和 Enter 重开可用；1920×1000 下概览宽 330、侧栏原生布局正常，互不覆盖。
- 中间宽度首次发现四列过挤和任务名溢出；已修正 4/2/1 列与列表项 min-width。概览高度受限并内部滚动，正文和输入框保有独立空间。
- 只读 UI 后仍是 5 次预设离线模型调用、父 1 轮；已结束 Agent 不恢复。未调用真实在线模型。最终浏览器 error 日志为空。

结果：本次界面验收通过。截图 `docs/assets/subagents-sidebar.png`、最终 run 中的 `subagents-narrow.png`。自动测试的分批展开验收覆盖 42 条目录；真实 Host UI 夹具为 4 条，不宣称做过 42 条真实 Host 测试。保留下方历史版本记录。
