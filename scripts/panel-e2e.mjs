/** Historical command alias; current product uses native conversation links. */
console.log('Use scripts/navigation-e2e.mjs; the floating panel is no longer mounted.')
await import('./navigation-e2e.mjs')
